# 多智能体文档生成系统实施方案

> **文档版本**: v1.0
> **创建日期**: 2026-02-28
> **适用场景**: 可研文件、初设文件、投标文件等大型专业文档生成

---

## 📋 目录

1. [项目背景与目标](#项目背景与目标)
2. [方案对比与选型](#方案对比与选型)
3. [推荐方案详解](#推荐方案详解)
4. [技术架构设计](#技术架构设计)
5. [实施计划](#实施计划)
6. [成本与风险评估](#成本与风险评估)
7. [验收标准](#验收标准)

---

## 项目背景与目标

### 业务需求

构建一个智能文档生成工作流系统，实现以下核心功能：

1. **素材库管理**：统一存储和索引多格式文档（PPT、Excel、Word、PDF、Markdown）
2. **智能检索**：混合搜索（向量+关键词）快速定位相关素材
3. **多智能体协同**：规划、研究、撰写、审核、编辑智能体协作生成大型文档
4. **文档输出**：生成可研文件、初设文件、投标文件等专业文档

### 关键发现

✅ **已有成熟的开源解决方案！** 无需从零开发，可基于现有项目快速构建。

---

## 方案对比与选型

<!--
配图建议：四象限对比图
Prompt: "Create a 2x2 quadrant diagram comparing 4 solutions: Kotaemon (top-right, high maturity & high fit), R2R (top-left, high maturity & medium fit), Morphik (bottom-right, medium maturity & high fit for technical docs), Custom Build (bottom-left, low maturity & full control). X-axis: Solution Fit, Y-axis: Maturity Level. Use professional colors."
-->

### 方案 A：基于 Kotaemon（⭐ 推荐）

**[Kotaemon](https://github.com/Cinnamon/kotaemon)** - 开源 RAG 文档问答系统

**核心优势**：

- ✅ 多用户支持（登录、私有/公共集合、协作分享）
- ✅ 混合 RAG（全文检索 + 向量搜索）
- ✅ 多模态支持（图表、表格的文档问答）
- ✅ 本地/云端灵活部署
- ✅ 开箱即用的 Web UI
- ✅ 活跃维护（2025年持续更新）

**需要扩展**：

- 添加多智能体文档生成模块（基于 LangGraph）
- 集成文档模板系统
- 添加导出功能（DOCX/PDF/PPTX）

**适用场景**：素材库管理 + 文档问答，需要扩展文档生成功能

---

### 方案 B：基于 R2R

**[R2R (SciPhi-AI)](https://github.com/SciPhi-AI/R2R)** - 生产就绪的智能体 RAG 系统

**核心优势**：

- ✅ Agentic RAG（内置智能体支持）
- ✅ RESTful API（完整的 API 接口）
- ✅ 生产级性能优化

**适用场景**：需要 API 集成和高性能的企业场景

---

### 方案 C：基于 Morphik

**[Morphik](https://www.morphik.ai/)** - 专为技术文档设计的多模态 RAG

**核心优势**：

- ✅ 视觉理解（理解图表、示意图、数据表）
- ✅ 技术文档优化（针对工程文档优化）
- ✅ 本地运行（完全本地化部署）

**适用场景**：大量技术图表的工程文档

---

### 方案 D：自研

基于 LangGraph + Qdrant 从零构建

**核心优势**：

- ✅ 完全可控（按需求定制）
- ✅ 最新技术栈

**劣势**：

- ⚠️ 开发周期长（6-8周）
- ⚠️ 维护负担重

---

### 对比分析表

| 维度                   | Kotaemon + 扩展 | R2R         | Morphik     | 自研     |
| ---------------------- | --------------- | ----------- | ----------- | -------- |
| **开发周期**     | 4-5周 ⭐        | 3-4周       | 5-6周       | 6-8周    |
| **素材库管理**   | ✅ 完整         | ⚠️ 需扩展 | ✅ 完整     | 需开发   |
| **多用户支持**   | ✅ 内置         | ⚠️ 基础   | ❌ 无       | 需开发   |
| **文档生成**     | 需扩展          | 需扩展      | 需扩展      | 完全定制 |
| **技术文档优化** | ⚠️ 一般       | ⚠️ 一般   | ✅ 优秀     | 可定制   |
| **生产就绪度**   | ✅ 高           | ✅ 很高     | ⚠️ 中等   | 需测试   |
| **社区支持**     | ✅ 活跃         | ✅ 活跃     | ⚠️ 新项目 | ❌ 无    |
| **可定制性**     | ⚠️ 中等       | ⚠️ 中等   | ⚠️ 中等   | ✅ 完全  |
| **学习曲线**     | 低 ⭐           | 中          | 中          | 高       |
| **维护成本**     | 低 ⭐           | 中          | 中          | 高       |

### 选型结论

🏆 **推荐方案：Kotaemon + LangGraph 扩展**

**理由**：

1. ✅ 最快上线（4-5周 vs 6-8周自研）
2. ✅ 功能最完整（80% 开箱即用）
3. ✅ 维护成本最低
4. ✅ 适合快速验证和迭代
5. ✅ 节省 50% 开发时间

---

## 推荐方案详解

### 核心思路

**复用 Kotaemon 现有能力（80%）+ 扩展 LangGraph 文档生成（20%）**

<!--
配图建议：系统架构分层图
Prompt: "Create a layered architecture diagram with 4 horizontal layers from top to bottom: 1) 'Kotaemon Web UI' (blue, existing) with icons for upload/manage/search/multi-user, 2) 'Kotaemon RAG Engine' (blue, existing) with document ingestion/hybrid search/vector store/multimodal, 3) 'LangGraph Document Generation' (orange, new) showing 5 agents in sequence: Planner → Researcher → Writer → Reviewer → Editor, 4) 'Document Export Module' (orange, new) with template engine/DOCX/PDF/PPTX generation. Use arrows to show data flow between layers. Label existing components in blue and new components in orange."
-->

### 架构设计

```
┌─────────────────────────────────────────────────────────┐
│         Kotaemon Web UI (现成)                          │
│  素材上传 | 文档管理 | 搜索测试 | 多用户                │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│         Kotaemon RAG 引擎 (现成)                        │
│  文档摄入 | 混合搜索 | 向量存储 | 多模态                │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│    新增：LangGraph 文档生成模块                         │
│  规划者 → 研究者 → 撰写者 → 审核者 → 编辑者            │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│    新增：文档导出模块                                   │
│  模板引擎 | DOCX/PDF/PPTX 生成 | 格式化                │
└─────────────────────────────────────────────────────────┘
```

**说明**：

- **蓝色部分（现成）**：Kotaemon 提供，无需开发
- **橙色部分（新增）**：需要扩展开发，约 2-3 周

---

### Kotaemon 核心功能（开箱即用）

**已实现功能**：

- ✅ 文档上传（PDF, DOCX, XLSX, PPTX, Markdown）
- ✅ 文档分块和嵌入
- ✅ 混合检索（全文 + 向量）
- ✅ 多用户系统（登录、权限、协作）
- ✅ 私有/公共集合
- ✅ 对话历史
- ✅ 多模态支持（图表、表格）
- ✅ 支持本地 LLM 和 API（OpenAI, Claude）
- ✅ 支持多种向量数据库（Qdrant, Milvus, Chroma）

**技术栈**：

- 前端：Gradio（可替换为 Streamlit）
- 后端：LangChain
- 向量数据库：可配置（推荐 Qdrant）
- 文档处理：Unstructured, PyMuPDF

---

### 需要扩展的模块

#### 1. LangGraph 文档生成模块

<!--
配图建议：LangGraph 状态流转图
Prompt: "Create a flowchart showing LangGraph agent workflow: Start → Planner (Claude Opus, creates outline) → Researcher (GPT-4, searches materials) → Writer (GPT-4, generates content) → Reviewer (Claude Opus, quality check) → Decision Diamond 'Approved?' → If Yes: Editor (GPT-4, final polish) → End; If No: back to Writer. Use different colors for each agent type and show the state being passed between nodes."
-->

**文件结构**：

```
kotaemon/
├── extensions/              # 新增扩展目录
│   ├── document_generation/
│   │   ├── agents/
│   │   │   ├── state.py          # 状态定义
│   │   │   ├── nodes.py          # 智能体节点
│   │   │   ├── graph.py          # 状态图
│   │   │   └── tools.py          # 工具函数
│   │   ├── templates/
│   │   │   ├── feasibility.json  # 可研模板
│   │   │   ├── design.json       # 初设模板
│   │   │   └── tender.json       # 投标模板
│   │   └── exporters/
│   │       ├── docx_exporter.py
│   │       ├── pdf_exporter.py
│   │       └── pptx_exporter.py
```

**集成方式**：

```python
# extensions/document_generation/api.py
from kotaemon.base import BaseComponent
from langgraph.graph import StateGraph

class DocumentGenerator(BaseComponent):
    """扩展 Kotaemon 的文档生成功能"""

    def __init__(self, kotaemon_app):
        self.app = kotaemon_app
        self.retriever = kotaemon_app.retriever  # 复用 Kotaemon 检索
        self.graph = self._build_graph()

    def generate_document(self, requirements: str, doc_type: str):
        """生成文档"""
        initial_state = {
            "requirements": requirements,
            "document_type": doc_type,
            "retriever": self.retriever  # 传入 Kotaemon 检索器
        }
        result = self.graph.invoke(initial_state)
        return result
```

**智能体配置**：

| 智能体                         | 模型        | 职责                                | 输出               |
| ------------------------------ | ----------- | ----------------------------------- | ------------------ |
| **规划者（Planner）**    | Claude Opus | 分析需求、创建大纲、分配任务        | 结构化大纲（JSON） |
| **研究者（Researcher）** | GPT-4       | Qdrant 混合搜索、提取信息、综合内容 | 研究结果集合       |
| **撰写者（Writer）**     | GPT-4       | 生成内容、格式化、引用来源          | 各章节内容         |
| **审核者（Reviewer）**   | Claude Opus | 质量评估、事实核查、一致性检查      | 评分 + 改进建议    |
| **编辑者（Editor）**     | GPT-4       | 整合反馈、最终润色、格式化输出      | 最终文档           |

**为什么选择 LangGraph**：

- ✅ 状态图架构：显式定义状态转换，易于调试和维护
- ✅ 生产就绪：2026年主流选择，社区活跃，文档完善
- ✅ 灵活控制：支持条件路由、循环、人工介入
- ✅ 可观测性：内置状态追踪和检查点机制

> **注意**：AutoGen 已于 2025年10月废弃，LangGraph 是当前生产环境首选

---

#### 2. Web UI 扩展

在 Kotaemon 的 Gradio UI 中添加新标签页：

```python
# app.py 扩展
import gradio as gr
from extensions.document_generation import DocumentGenerator

# 原有 Kotaemon UI
with gr.Blocks() as demo:
    with gr.Tab("Chat"):
        # Kotaemon 原有聊天界面
        pass

    with gr.Tab("Document Generation"):  # 新增
        gr.Markdown("## 文档生成")
        doc_type = gr.Dropdown(["可研文件", "初设文件", "投标文件"])
        requirements = gr.Textbox(label="需求描述", lines=5)
        generate_btn = gr.Button("生成文档")
        output = gr.File(label="下载文档")

        generate_btn.click(
            fn=doc_generator.generate_document,
            inputs=[requirements, doc_type],
            outputs=output
        )
```

---

## 技术架构设计

### 技术栈总结

**复用 Kotaemon**：

- 文档管理和存储
- 文档摄入和分块
- 混合检索引擎
- 多用户系统
- Web UI 框架

**新增技术**：

- LangGraph（多智能体）
- python-docx（Word 导出）
- ReportLab（PDF 导出）
- python-pptx（PPT 导出）
- Jinja2（模板引擎）

### 向量数据库选型：Qdrant

**为什么选择 Qdrant**：

- ✅ 生产级性能：高效的 HNSW 索引，支持百万级向量
- ✅ 复杂过滤：支持元数据过滤、混合搜索
- ✅ 本地部署：Docker 一键启动，开发环境与生产一致
- ✅ 易于集成：Python 客户端简洁，与 LangChain 无缝集成

**Qdrant 集成示例**：

```python
from qdrant_client import QdrantClient
from qdrant_client.models import Distance, VectorParams

# 初始化客户端（本地 Docker）
client = QdrantClient(host="localhost", port=6333)

# 创建集合
client.create_collection(
    collection_name="documents",
    vectors_config=VectorParams(size=384, distance=Distance.COSINE)
)

# 混合搜索
results = client.search(
    collection_name="documents",
    query_vector=embedding,
    query_filter={
        "must": [
            {"key": "document_type", "match": {"value": "pdf"}},
            {"key": "created_at", "range": {"gte": "2024-01-01"}}
        ]
    },
    limit=10
)
```

---

## 实施计划

<!--
配图建议：甘特图
Prompt: "Create a Gantt chart showing 5 phases over 5 weeks: Week 1 'Deploy Kotaemon + Evaluation', Week 2-3 'Develop LangGraph Module' (longest bar), Week 4 'UI Integration + Templates', Week 5 'Testing & Deployment'. Use different colors for each phase and show dependencies with arrows."
-->

### Phase 1: 部署 Kotaemon（1-2天）

**任务清单**：

```bash
# 1. 克隆仓库
git clone https://github.com/Cinnamon/kotaemon
cd kotaemon

# 2. 安装依赖
pip install -e ".[dev]"

# 3. 配置 Qdrant（Docker）
docker run -p 6333:6333 -v $(pwd)/qdrant_storage:/qdrant/storage qdrant/qdrant

# 4. 配置 LLM（编辑 .env）
OPENAI_API_KEY=your_key
ANTHROPIC_API_KEY=your_key

# 5. 启动
python app.py
```

**验证标准**：

- [ ] 上传测试文档（PDF, DOCX, XLSX）
- [ ] 测试文档问答
- [ ] 测试多用户登录
- [ ] 测试搜索功能

---

### Phase 2: 评估和定制（1周）

**任务清单**：

- [ ] 测试文档上传和检索质量
- [ ] 评估多用户功能是否满足需求
- [ ] 配置向量数据库（Qdrant）
- [ ] 集成 Claude/GPT-4 API
- [ ] 上传 20-30 个真实文档进行测试

**决策点**：

- 检索质量是否满足需求？
- 扩展难度是否可接受？
- 决定：继续扩展 or 换方案

---

### Phase 3: 扩展文档生成（2-3周）

#### Week 1: LangGraph 智能体

**任务清单**：

- [ ] 定义 DocumentState 状态模式
- [ ] 实现 5 个智能体节点（Planner, Researcher, Writer, Reviewer, Editor）
- [ ] 构建状态图和条件路由
- [ ] 集成 Kotaemon 检索器
- [ ] 单元测试

**关键代码示例**：

```python
from langgraph.graph import StateGraph, END
from typing import TypedDict

class DocumentState(TypedDict):
    requirements: str
    document_type: str
    outline: dict
    sections: dict
    final_document: str
    retriever: Any  # Kotaemon 检索器

# 构建图
workflow = StateGraph(DocumentState)
workflow.add_node("planner", planner_node)
workflow.add_node("researcher", researcher_node)
workflow.add_node("writer", writer_node)
workflow.add_node("reviewer", reviewer_node)
workflow.add_node("editor", editor_node)

# 定义工作流
workflow.set_entry_point("planner")
workflow.add_edge("planner", "researcher")
workflow.add_edge("researcher", "writer")
workflow.add_edge("writer", "reviewer")

# 条件路由：审核通过 → 编辑，否则 → 重写
workflow.add_conditional_edges(
    "reviewer",
    should_continue,
    {"editor": "editor", "writer": "writer"}
)

workflow.add_edge("editor", END)
app = workflow.compile()
```

#### Week 2: 模板和导出

**任务清单**：

- [ ] 创建文档模板（可研、初设、投标）
- [ ] 实现 DOCX 导出器
- [ ] 实现 PDF 导出器
- [ ] 实现 PPTX 导出器（可选）
- [ ] 集成测试

---

### Phase 4: UI 集成 + 模板系统（1周）

**任务清单**：

- [ ] 在 Kotaemon UI 添加"文档生成"标签页
- [ ] 需求输入界面
- [ ] 进度显示（实时显示智能体状态）
- [ ] 结果预览和下载
- [ ] 用户测试

---

### Phase 5: 测试优化 + 部署（1周）

**任务清单**：

- [ ] 性能优化（批量处理、缓存）
- [ ] 错误处理和重试机制
- [ ] 端到端测试
- [ ] 用户文档编写
- [ ] 部署到生产环境

---

## 成本与风险评估

### 成本估算

**方案 A：Kotaemon + 扩展**

- **开发时间**：4-5周
- **人力成本**：1-2 名开发者
- **基础设施**：
  - Qdrant Docker（免费）
  - Claude API（按使用付费，约 $15/百万 tokens）
  - GPT-4 API（按使用付费，约 $30/百万 tokens）
- **总成本**：**最低** ⭐

**方案 D：自研**

- **开发时间**：6-8周
- **人力成本**：2-3 名开发者
- **基础设施**：同上
- **总成本**：**最高**（2-3倍）

### 风险评估

**Kotaemon 方案风险**：

| 风险                 | 影响 | 缓解措施                   |
| -------------------- | ---- | -------------------------- |
| 依赖第三方项目       | 中   | 活跃维护，可 fork 自己维护 |
| 扩展可能与主分支冲突 | 低   | 模块化设计，独立扩展目录   |
| 技术风险             | 低   | 成熟技术栈，社区支持       |

**自研方案风险**：

| 风险             | 影响 | 缓解措施     |
| ---------------- | ---- | ------------ |
| 开发周期不确定   | 高   | -            |
| Bug 和稳定性问题 | 高   | 需要大量测试 |
| 维护负担重       | 高   | 需要专人维护 |

---

## 验收标准

### 功能测试

- [ ] 上传 100+ 个测试文档
- [ ] 测试混合搜索准确率（目标 > 80%）
- [ ] 生成一份完整可研文件
- [ ] 多用户协作测试
- [ ] 导出格式验证（DOCX/PDF）

### 性能测试

- [ ] 文档摄入速度（目标 < 30秒/文档）
- [ ] 检索响应时间（目标 < 2秒）
- [ ] 文档生成时间（目标 < 5分钟/万字）
- [ ] 并发用户支持（目标 10+ 用户）

### 质量评估

- [ ] 检索相关性评分（目标 > 80%）
- [ ] 生成内容准确性（人工评估）
- [ ] 引用来源正确性（目标 > 95%）
- [ ] 格式规范性（符合模板要求）

---

## 快速验证方案（1周 POC）

如果不确定是否采用 Kotaemon，可以先做 1 周 POC：

**Day 1-2: 部署 Kotaemon**

- 安装和配置
- 上传 20-30 个测试文档
- 测试检索质量

**Day 3-4: 简单扩展**

- 实现单个智能体（Writer）
- 测试从 Kotaemon 检索 → 生成内容
- 导出简单 Markdown

**Day 5: 评估决策**

- 检索质量是否满足需求？
- 扩展难度是否可接受？
- 决定：继续扩展 or 换方案

---

## 替代方案

### Plan B: R2R + 自定义 UI

如果 Kotaemon 不合适：

- 使用 R2R 作为 RAG 后端（API）
- 自己开发 Streamlit UI
- 添加 LangGraph 文档生成
- **开发周期**：5-6周

### Plan C: 完全自研

如果有特殊需求：

- 按照原计划从零构建
- 使用 LangGraph + Qdrant + Streamlit
- **开发周期**：6-8周

---

## 参考资源

### Kotaemon

- [GitHub 仓库](https://github.com/Cinnamon/kotaemon)
- [官方文档](https://cinnamon.github.io/kotaemon/)
- [Kotaemon 完整指南 2025](https://skywork.ai/blog/kotaemon-comprehensive-guide-2025-everything-you-need-to-know/)

### 其他参考项目

- [R2R - 生产级 Agentic RAG](https://github.com/SciPhi-AI/R2R)
- [Morphik - 技术文档 RAG](https://www.morphik.ai/)
- [Agentset - RAG 基础设施](https://docs.agentset.ai/)

### LangGraph

- [官方文档](https://docs.langchain.com/oss/python/langgraph/overview)
- [Thinking in LangGraph](https://docs.langchain.com/oss/python/langgraph/thinking-in-langgraph)
- [Agent Orchestration 2026 Guide](https://iterathon.tech/blog/ai-agent-orchestration-frameworks-2026)

### RAG 和向量数据库

- [Best Vector Databases for Production RAG 2026](https://engineersguide.substack.com/p/best-vector-databases-rag)
- [RAG Systems Production Guide 2026](https://iterathon.tech/blog/rag-systems-production-guide-2025)

---

## 附录：关键文件清单

**Kotaemon 核心文件**：

- `kotaemon/base/retriever.py` - 检索器接口
- `kotaemon/indices/` - 索引和向量存储
- `kotaemon/loaders/` - 文档加载器
- `app.py` - 主应用入口

**需要创建的扩展文件**：

- `extensions/document_generation/agents/graph.py` - LangGraph 状态图
- `extensions/document_generation/templates/` - 文档模板
- `extensions/document_generation/exporters/` - 导出器
- `extensions/document_generation/api.py` - API 接口

**配置文件**：

- `.env` - 环境变量（API keys）
- `docker-compose.yml` - Qdrant 容器
- `config.yaml` - Kotaemon 配置

---

## 快速启动命令

```bash
# 1. 安装依赖
pip install langgraph langchain qdrant-client sentence-transformers

# 2. 启动 Qdrant
docker run -p 6333:6333 -v $(pwd)/qdrant_storage:/qdrant/storage qdrant/qdrant

# 3. 克隆 Kotaemon
git clone https://github.com/Cinnamon/kotaemon
cd kotaemon
pip install -e ".[dev]"

# 4. 配置环境变量
echo "OPENAI_API_KEY=your_key" > .env
echo "ANTHROPIC_API_KEY=your_key" >> .env

# 5. 启动应用
python app.py
```

---

**文档结束**

> 💡 **提示**：本文档中标注了配图建议的位置，可以使用提供的 Prompt 在 AI 绘图工具（如 Midjourney、DALL-E）中生成相应的架构图和流程图，以便向领导展示。
